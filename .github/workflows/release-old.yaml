# name: Release (old)

# concurrency: release

# on:
#   push:
#     branches:
#       - master
#   workflow_dispatch:

# env:
#   CARGO_TERM_COLOR: always

# permissions:
#   contents: write
#   pages: write
#   id-token: write

# jobs:
#   release:
#     name: Bump version, generate docs, and package crate

#     runs-on: ubuntu-latest

#     outputs:
#       crate-version: ${{ steps.crate_version.outputs.version }}

#     steps:
#       - name: Generate a token
#         id: generate_token
#         uses: actions/create-github-app-token@v3
#         with:
#           client-id: ${{ secrets.PSW_CI_APP_ID }}
#           private-key: ${{ secrets.PSW_CI_PRIVATE_KEY }}

#       - name: Checkout
#         uses: actions/checkout@v7
#         with:
#           fetch-depth: 0
#           fetch-tags: true
#           show-progress: true
#           submodules: recursive
#           token: ${{ steps.generate_token.outputs.token }}

#       - name: Set up Rust toolchain
#         uses: dtolnay/rust-toolchain@stable

#       - name: Cache dependencies
#         uses: Swatinem/rust-cache@v2

#       - name: Read crate version
#         id: crate_version
#         run: |
#           version=$(grep -m1 '^version = ' Cargo.toml | sed -E 's/version = "(.*)"/\1/')
#           echo "version=$version" >> "$GITHUB_OUTPUT"

#       - name: Cargo release
#         id: cargo_release
#         env:
#           GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}
#         run: |
#           cargo install --quiet cargo-smart-release
#           git config --global user.email "ci@pilgrimagesoftware.com"
#           git config --global user.name "Pilgrimage Software CI"
#           cargo smart-release \
#             --update-crates-index \
#             --allow-fully-generated-changelogs \
#             --no-bump-on-demand \
#             --no-tag \
#             --no-push \
#             --execute

#       - name: Generate documentation
#         run: |
#           cargo doc --no-deps --verbose

#       - name: Add redirect index
#         run: |
#           echo '<meta http-equiv="refresh" content="0; url=dtrpg_sdk/index.html">' \
#             > target/doc/index.html

#       - name: Package
#         run: |
#           cargo package --verbose --no-verify --allow-dirty

#       - name: Upload package artifact
#         uses: actions/upload-artifact@v7
#         with:
#           name: crate-${{ github.sha }}
#           path: target/package/*.crate
#           retention-days: 30

#       - name: Upload documentation artifact
#         uses: actions/upload-artifact@v7
#         with:
#           name: docs-${{ github.sha }}
#           path: target/doc/
#           retention-days: 30

#       - name: Upload Pages artifact
#         # if: github.ref == 'refs/heads/master'
#         uses: actions/upload-pages-artifact@v5
#         with:
#           path: target/doc/

#   publish:
#     name: Publish crate and create GitHub Release
#     needs: release
#     if: github.ref == 'refs/heads/master'
#     runs-on: ubuntu-latest

#     permissions:
#       contents: write

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v7
#         with:
#           fetch-depth: 0
#           fetch-tags: true
#           show-progress: true
#           submodules: recursive

#       - name: Set up Rust toolchain
#         uses: dtolnay/rust-toolchain@stable

#       - name: Download package artifact
#         uses: actions/download-artifact@v7
#         with:
#           name: crate-${{ github.sha }}
#           path: target/package

#       - name: Download documentation artifact
#         uses: actions/download-artifact@v7
#         with:
#           name: docs-${{ github.sha }}
#           path: target/doc

#       - name: Archive documentation
#         run: |
#           tar -czf "$RUNNER_TEMP/docs-${{ needs.release.outputs.crate-version }}.tar.gz" -C target/doc .

#       - name: Create GitHub Release
#         uses: softprops/action-gh-release@v2
#         with:
#           tag_name: ${{ needs.release.outputs.crate-version }}
#           name: v${{ needs.release.outputs.crate-version }}
#           files: |
#             target/package/*.crate
#             ${{ runner.temp }}/docs-${{ needs.release.outputs.crate-version }}.tar.gz
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#       - name: Publish to crates.io
#         env:
#           CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_API_KEY }}
#         run: |
#           cargo publish --verbose

#   deploy-docs:
#     name: Deploy documentation to GitHub Pages
#     needs: release
#     # if: github.ref == 'refs/heads/master'
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}

#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v5
